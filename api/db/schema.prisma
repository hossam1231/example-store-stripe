datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:12nhRCTKKcCR441ZT4D9@containers-us-west-77.railway.app:5919/railway"
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = "native"
}

model User {
  id                  String    @id
  email               String    @unique
  name                String?
  hashedPassword      String
  salt                String
  resetToken          String?
  resetTokenExpiresAt DateTime?
}
